# 2026-08-04 工作记录

## 每日备份到夸克网盘（自动化 automation-1785512468358）
- 直接 `node` 调用 `C:/Users/Beekay/.quarkclouddrive/scripts/quark-drive.cjs`（绕过启动器/install.sh），node 用托管路径 `C:/Users/Beekay/.workbuddy/binaries/node/versions/22.22.2/node.exe`（沙箱无 node）。
- 目标 fid `72d6817a65e4432280b01304ee30920d`（「宝可梦学习平台备份」），session-id `auto-bak-daily`。
- 上传 12 个顶层项（index.html / assets / .github / .gitignore / README.md / android / functions / icons / tools / scripts / manifest.webmanifest / worker.js），排除 .git、.workbuddy、运行时临时文件（_dl.log、_gen2_names.txt、_syntax_check.txt、index.html.bak.inject）。
- 当前工程规模：本地上传目标共 **1091 个文件**（assets 含 1043 个 png/webp，占绝大多数）；去重后 **1087 个不重复 basename**（4 个为跨目录同名碰撞）。
- 结果：所有文件均 `code:0 / instantUpload:true`（内容已在云端，秒传命中）。CLI 末尾状态上报调用在限流下 `ECONNABORTED (code:1007)` 中断，但**数据完整、无文件缺失**（本地 vs 日志 basename 差集为空）。
- 限流提示：单次全量 1091 文件易触发账号级限流，需多次重跑+冷却；CLI 成功计数字段因末尾中断未输出，改用「日志 basename 与本地差集」做完整性校验更可靠。
